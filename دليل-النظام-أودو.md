# نظام إبراهيم سعود (محرّك أودو ١٩ Community)

نظام كامل على جهازك، بقاعدة بياناتك، بدون أي اشتراك أو رسوم لأودو.
الرخصة LGPLv3 — استخدام تجاري مسموح، والعلامة التجارية أُزيلت من الواجهة بالكامل.

---

## التشغيل

```bash
./odoo.sh              # تشغيل النظام ويفتح المتصفح
./odoo.sh stop         # إيقاف
./odoo.sh restart      # إعادة تشغيل
./odoo.sh log          # متابعة السجل مباشرة
./odoo.sh update is_brand   # تحديث وحدة بعد تعديلها
./odoo.sh shell        # صدفة بايثون داخل النظام
```

العنوان: <http://127.0.0.1:8069>

## الدخول

المستخدم وكلمة المرور في ملف **`.odoo-secrets`** داخل المجلد
(مستثنى من git فلا يُرفع مع الموقع أبداً).

```bash
cat .odoo-secrets
```

> غيّر كلمة المرور من: صورة المستخدم ← تفضيلاتي ← الأمان.

---

## ما المُثبَّت

**١٢٦ وحدة** تشمل:

| المجال | الوحدة |
|---|---|
| العملاء والفرص | جهات الاتصال، إدارة علاقات العملاء |
| المبيعات | عروض أسعار، أوامر بيع، اشتراكات زمنية |
| المحاسبة والفوترة | شجرة حسابات سعودية، ضريبة ١٥٪، فوترة زاتكا الإلكترونية |
| المخزون | مستودعات، حركات، جرد (مناسب للمعدّات) |
| المشتريات | موردون، أوامر شراء |
| نقطة البيع | كاشير باللمس + إيصالات زاتكا |
| المشاريع | مهام، جداول زمنية (ساعات العمل على المشاريع) |
| الموارد البشرية | موظفون، حضور |

الشركة: **إبراهيم سعود** — السعودية، الريال، توقيت الرياض، الواجهة عربية RTL.

---

## البنية على جهازك

| المكوّن | المسار |
|---|---|
| سورس النظام | `odoo-19.0/` (لا يُرفع لـ git) |
| بيئة بايثون ٣.١٢ | `odoo-venv/` |
| الإعدادات | `odoo.conf` |
| البيانات والمرفقات والسجل | `odoo-data/` |
| **وحدة هويتك** | `odoo-addons/is_brand/` ← **مرفوعة مع git** |
| قاعدة البيانات | PostgreSQL 16 محلي، قاعدة `ibrahimsaud` |
| بيانات القاعدة | `~/Library/Application Support/Postgres/var-16` |

قاعدة البيانات تعمل عبر Postgres.app — تقدر تشغّلها/توقفها من شريط القوائم،
أو `./odoo.sh` يشغّلها تلقائياً.

---

## وحدة الهوية `is_brand`

| ماذا | كيف |
|---|---|
| الألوان | حبري `#0a0a0c` + ذهبي `#e7b24c` + كريمي `#f5f2ea` |
| الخطوط | ثمانية Sans للنصوص، ثمانية Serif للعناوين |
| الشعار | شعارك في الشركة وصفحة الدخول والأيقونة المفضّلة |
| عنوان المتصفح | «نظام إبراهيم سعود» |
| إزالة العلامة | صفحة الدخول، تذييل الرسائل، قائمة المستخدم، إعلانات الترقية |
| منظّف المصطلحات | `models/ir_http.py` يستبدل أي ذكر للمنصّة في نصوص الواجهة بـ«النظام» |

بعد أي تعديل على الوحدة: `./odoo.sh update is_brand`

### تغيير الألوان
عدّل `odoo-addons/is_brand/static/src/scss/primary_variables.scss` ثم حدّث الوحدة.

---

## نسخة احتياطية

```bash
# حفظ نسخة من قاعدة البيانات
/Applications/Postgres.app/Contents/Versions/16/bin/pg_dump -p 5432 ibrahimsaud \
  -Fc -f ~/Desktop/ibrahimsaud-odoo-$(date +%Y%m%d).dump

# المرفقات والملفات
cp -R odoo-data/filestore ~/Desktop/filestore-backup
```

---

## ملاحظات

- النظام يعمل على `127.0.0.1` فقط (لا يُرى من خارج جهازك). لفتحه للشبكة
  أو لربط نظامك المنشور عليه، يحتاج استضافة وعنوان ونطاق — خطوة منفصلة.
- المصادقة عبر LDAP غير مثبّتة (مكتبتها تحتاج ترويسات لا تتوفر على macOS بدون Homebrew) — لا علاقة لها باستخدامك.
- طباعة PDF: إن احتجت تقارير PDF بجودة عالية، يلزم تثبيت `wkhtmltopdf`.
