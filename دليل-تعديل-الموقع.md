# 📘 دليل تعديل موقع ibrahimsaud.com بنفسك

دليل مبسّط — مو محتاج تكون مبرمج. أغلب المحتوى تعدّله من **ملف واحد** اسمه `src/lib/site.ts`، والنشر يصير تلقائياً.

---

## ✨ الطريقة الأسهل: التعديل مباشرة من GitHub

ما تحتاج terminal ولا أوامر. كل تعديل يمر بنفس الخطوات:

1. ادخل مستودعك: **github.com/ibrahimsaud-255/ibrahimsaud-site**
2. افتح الملف: مجلد `src` ← `lib` ← **`site.ts`**
3. اضغط زر **القلم ✏️** (فوق يمين الملف، جنب كلمة Raw).
4. عدّل النص اللي تبيه.
5. اضغط الزر الأخضر **«Commit changes»** (فوق يمين).
6. في النافذة المنبثقة، اضغط **«Commit changes»** مرة ثانية.
7. خلاص! الموقع يحدّث نفسه خلال دقيقة تقريباً.

**تتأكد إن النشر تم:** افتح تبويب **Actions** فوق — لو طلعت علامة ✅ خضراء، التعديل نُشر. لو ❌ حمراء، فيه خطأ (راجع «إذا خربت» تحت).

---

## 🗂️ وش تلقى في ملف `site.ts`؟

| القسم | وش فيه |
|------|--------|
| `name` / `tagline` / `bio` | اسمك، العبارة التعريفية، النبذة |
| `whatsapp` / `email` | رقم الواتساب والبريد |
| `social` | روابط تيك توك، إنستقرام، يوتيوب، إكس |
| `ventures` | مشاريعك الأخرى (بطاقات) |
| `services` | الخدمات اللي تقدّمها |
| `processSteps` | خطوات «كيف نشتغل» |
| `works` | الأعمال + روابط الفيديوهات |

---

## ✏️ أمثلة عملية على التعديل

### تغيير نص (مثلاً النبذة)
لقّط السطر:
```
bio: "إبراهيم سعود | تقنية أعمال بودكاست",
```
غيّر اللي **بين علامتي التنصيص** بس:
```
bio: "إبراهيم سعود | صانع محتوى ومنتج إعلانات",
```

### إضافة رابط إنستقرام
لقّط:
```
instagram: "",
```
حط الرابط بين القوسين:
```
instagram: "https://instagram.com/حسابك",
```

### إضافة فيديو لعمل موجود
دوّر العمل (مثلاً المفك السحري)، ولقّط:
```
videoUrl: "",
```
بدّلها بـ:
```
videos: ["https://youtu.be/رمز_الفيديو"],
```

### إضافة أكثر من فيديو لنفس العمل
```
videos: [
  "https://youtu.be/الفيديو_الأول",
  "https://youtu.be/الفيديو_الثاني",
],
```

### إضافة كواليس لعمل
أضف تحت سطر الـ videos:
```
bts: "https://youtu.be/رابط_الكواليس",
```

> الروابط المدعومة: يوتيوب (عادي/Shorts)، تيك توك، Vimeo، أو ملف mp4 مباشر.

---

## ⚠️ قواعد ذهبية (عشان ما يخرب الكود)

- **لا تشيل علامات التنصيص** `" "` حول النص — غيّر اللي بينها فقط.
- **خلّ الفاصلة** `,` في آخر كل سطر مثل ما هي.
- **لا تحذف الأقواس** `{ }` ولا `[ ]`.
- عدّل سطر واحد في كل مرة لو كنت مبتدئ — أسهل في المتابعة.

---

## 🛟 إذا خربت أو غلطت

- تبويب **Actions** يطلع علامة ❌ حمراء = فيه خطأ، والموقع ما يتحدّث (نسخته القديمة تبقى شغّالة، فلا تخاف).
- للرجوع لنسخة سليمة: افتح الملف ← زر **History** فوق ← اختر نسخة قبل الخطأ.
- أو ببساطة راسلني وأصلحها لك بثوانٍ.

---

## 🖼️ إضافة صور وشعارات

1. ارفع الصورة في مجلد **`public`** داخل المستودع (Add file ← Upload files).
2. سمّها باسم إنجليزي بسيط بدون مسافات، مثل `logo-aramco.png`.
3. اربطها في `site.ts` بالشكل: `logo: "/logo-aramco.png",` (لاحظ الشرطة المائلة في البداية).

---

## 💻 الطريقة الثانية: VS Code (للتعديلات الكبيرة)

تفيدك لو تبي تشوف الموقع على جهازك قبل النشر:

- معاينة محلية: في الـterminal اكتب `npm run dev` ثم افتح `http://localhost:3000`.
- بعد التعديل، النشر: `git add -A` ثم `git commit -m "تعديل"` ثم `git push`.
- تنبيه: لو عدّلت من GitHub والـVS Code بالتناوب، شغّل `git pull` أول قبل ما تبدأ في VS Code عشان تجيب آخر نسخة.

> للشغل اليومي البسيط، التعديل من GitHub مباشرة أسهل وأأمن لك.

---

## 🆘 متى تحتاجني؟

- تصميم جديد أو تغيير شكل الموقع.
- خطأ ما عرفت تصلحه.
- إضافة قسم أو ميزة جديدة.

أي شي ثاني، أنا موجود. بالتوفيق! 🚀
